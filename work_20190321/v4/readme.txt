本次主要进行文件系统的优化：之前编译后生产的文件特别乱，这次全部放到一个build文件里面
1.打开test.cpp程序，将两个相对路径更改为绝对路径
2.在该文件夹下调用命令行
3.创建build文件夹专门放置编译运行的文件"mkdir build"
4.进入build文件夹"cd build"
5.使用上一层文件进行编译"cmake .."
6.生产可执行文件"make"
7.运行"./test"
大功告成，整个文件系统看起来简介明了
